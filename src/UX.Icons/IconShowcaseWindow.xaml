<Window x:Class="UX.Icons.IconShowcaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icons="clr-namespace:UX.Icons"
        Title="Tabler Icons Showcase" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock Text="Tabler Icons Showcase" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TotalCountText" Text="(0 icons)" FontSize="16" Foreground="Gray" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Search Box -->
                <TextBox x:Name="SearchBox" 
                         Grid.Row="1"
                         Height="35"
                         Padding="10,0"
                         Margin="0,15,0,0"
                         VerticalContentAlignment="Center"
                         FontSize="14"
                         TextChanged="SearchBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="White" 
                                                BorderBrush="#D0D0D0" 
                                                BorderThickness="1" 
                                                CornerRadius="4">
                                            <Grid>
                                                <TextBlock Text="Search icons..." 
                                                          Foreground="#999" 
                                                          Margin="10,0,0,0"
                                                          VerticalAlignment="Center"
                                                          IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="10,0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Border>
        
        <!-- Icons Grid -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled"
                      PanningMode="VerticalOnly">
            <ItemsControl x:Name="IconsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="20,10,20,20">
                            <!-- Letter Header -->
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Letter}" 
                                            FontSize="24" 
                                            FontWeight="Bold" 
                                            Foreground="Black"
                                            VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Count, StringFormat=' ({0} icons)'}" 
                                            FontSize="14" 
                                            Foreground="Black" 
                                            Opacity="0.9"
                                            Margin="10,0,0,0"
                                            VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <!-- Icons in this letter group -->
                            <WrapPanel>
                                <ItemsControl ItemsSource="{Binding Icons}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="White" 
                                                    BorderBrush="#E0E0E0" 
                                                    BorderThickness="1" 
                                                    CornerRadius="8" 
                                                    Padding="15"
                                                    Margin="5"
                                                    Width="140" 
                                                    Height="140"
                                                    Cursor="Hand"
                                                    ToolTip="{Binding}"
                                                    MouseLeftButtonDown="IconCard_Click">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="5" 
                                                                                ShadowDepth="2" 
                                                                                Opacity="0.1" 
                                                                                Color="Black"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                                                <Setter Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect BlurRadius="10" 
                                                                                        ShadowDepth="3" 
                                                                                        Opacity="0.2" 
                                                                                        Color="#2196F3"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <icons:Icon IconName="{Binding}" 
                                                               IconSize="48" 
                                                               Foreground="#333"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                    
                                                    <TextBlock x:Name="CopiedText"
                                                              Grid.Row="1"
                                                              Text="✓ Copied!"
                                                              FontSize="10"
                                                              Foreground="#4CAF50"
                                                              FontWeight="Bold"
                                                              TextAlignment="Center"
                                                              Margin="0,5,0,5"
                                                              Visibility="Collapsed"/>
                                                    
                                                    <TextBlock Grid.Row="2" 
                                                              Text="{Binding}" 
                                                              FontSize="11" 
                                                              Foreground="#666"
                                                              TextAlignment="Center"
                                                              TextWrapping="Wrap"
                                                              Margin="0,5,0,0"
                                                              MaxHeight="40"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </WrapPanel>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
