<UserControl x:Class="ScreenShare.UX.Controls.ParticipantsListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScreenShare.UX.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="250">
    <Border Background="White" Padding="10" BorderThickness="0,1,0,0" BorderBrush="#CCCCCC">
        <StackPanel>
            <TextBlock Text="Participants" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock x:Name="ParticipantCountTextBlock" Foreground="#666666" FontSize="12" Margin="0,0,0,5"/>
            
            <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ParticipantsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#E8F5E9" Padding="5" CornerRadius="3" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}" FontSize="12" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" 
                                            Content="X" 
                                            Width="20" 
                                            Height="20" 
                                            FontSize="14"
                                            Padding="0"
                                            Click="RemoveParticipant_Click"
                                            Tag="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMainUser}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
            <!-- Test buttons -->
            <Button x:Name="AddTestUserButton" Content="Add Test User" Margin="0,10,0,0" Padding="5"/>
        </StackPanel>
    </Border>
</UserControl>
