<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Communicator.Icons">

    <!-- Default style for Icon control -->
    <Style TargetType="{x:Type local:Icon}">
        <Setter Property="FontFamily" Value="/Communicator.Icons;component/Assets/Fonts/#tabler-icons" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Width" Value="{Binding IconSize, RelativeSource={RelativeSource Self}}" />
        <Setter Property="Height" Value="{Binding IconSize, RelativeSource={RelativeSource Self}}" />
        <Setter Property="SnapsToDevicePixels" Value="False" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Icon}">
                    <Grid>
                        <TextBlock 
                            Text="{TemplateBinding Glyph}"
                            FontFamily="{TemplateBinding FontFamily}"
                            Foreground="{TemplateBinding Foreground}"
                            FontSize="{TemplateBinding IconSize}"
                            TextAlignment="Center"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            TextOptions.TextFormattingMode="Ideal"
                            TextOptions.TextRenderingMode="Auto"
                            RenderOptions.EdgeMode="Unspecified"
                            RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
