<UserControl x:Class="Communicator.UX.Views.Common.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#AA000000">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressBar IsIndeterminate="{Binding IsBusy}" Width="200" Height="4" Margin="0,0,0,20"
                         Foreground="#007ACC" Background="#333333" BorderThickness="0"/>
            <TextBlock Text="{Binding Message}" HorizontalAlignment="Center" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</UserControl>
