<Application x:Class="Communicator.UX.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:authVm="clr-namespace:Communicator.UX.ViewModels.Auth"
             xmlns:authViews="clr-namespace:Communicator.UX.Views.Auth"
             xmlns:homeVm="clr-namespace:Communicator.UX.ViewModels.Home"
             xmlns:meetingVm="clr-namespace:Communicator.UX.ViewModels.Meeting"
             xmlns:settingsVm="clr-namespace:Communicator.UX.ViewModels.Settings"
             xmlns:homeViews="clr-namespace:Communicator.UX.Views.Home"
             xmlns:meetingViews="clr-namespace:Communicator.UX.Views.Meeting"
             xmlns:settingsViews="clr-namespace:Communicator.UX.Views.Settings"
             xmlns:converters="clr-namespace:Communicator.Core.UX.Converters;assembly=Communicator.Core">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Theme Resources - LightTheme loaded by default, dynamically switched by ThemeService -->
                <ResourceDictionary Source="/Communicator.Core;component/UX/Themes/LightTheme.xaml"/>
                
                <!-- Control Styles -->
                <ResourceDictionary Source="/Communicator.Core;component/UX/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Converters -->
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
            
            <!-- Data Template for Auth View (Google OAuth only) -->
            <DataTemplate DataType="{x:Type authVm:AuthViewModel}">
                <authViews:AuthView />
            </DataTemplate>
            
            <!-- Data Templates for Application Views -->
            <DataTemplate DataType="{x:Type homeVm:HomePageViewModel}">
                <homeViews:HomePageView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type settingsVm:SettingsViewModel}">
                <settingsViews:SettingsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:MeetingToolbarViewModel}">
                <meetingViews:MeetingToolbarView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:MeetingShellViewModel}">
                <meetingViews:MeetingShellView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:AIInsightsViewModel}">
                <meetingViews:AIInsightsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:VideoSessionViewModel}">
                <meetingViews:VideoSessionView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:ScreenShareViewModel}">
                <meetingViews:ScreenShareView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:WhiteboardViewModel}">
                <meetingViews:WhiteboardView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type meetingVm:MeetingChatViewModel}">
                <meetingViews:MeetingChatView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>