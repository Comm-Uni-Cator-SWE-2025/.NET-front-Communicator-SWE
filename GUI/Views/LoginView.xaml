<UserControl x:Class="GUI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <StackPanel MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="48">
            <TextBlock Text="Welcome Back" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
            <TextBlock Text="Use your IIT Palakkad Academic Email to Login"
                       FontSize="14"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="0,10,0,24" />

            <TextBlock Text="Email" 
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,8" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBoxStyle}"
                     Margin="0,0,0,20"
                     Height="44"
                     HorizontalAlignment="Stretch" />

            <TextBlock Text="Password" 
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,8" />
            <PasswordBox x:Name="PasswordBox"
                         Style="{StaticResource ModernPasswordBoxStyle}"
                         Margin="0,0,0,28"
                         Height="44"
                         PasswordChanged="PasswordBox_PasswordChanged" />

            <Button Content="Sign In"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Height="44"
                    Margin="0,0,0,12"
                    HorizontalAlignment="Stretch" />
            <Button Content="Sign In with Google"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Height="44"
                    Margin="0,0,0,20"
                    HorizontalAlignment="Stretch" />
            <Button Content="Don't have an account? Sign Up here"
                    Command="{Binding GoToSignUpCommand}"
                    Height="44"
                    HorizontalAlignment="Stretch"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{DynamicResource PrimaryBrush}"
                    Cursor="Hand"
                    FontWeight="Medium" />
        </StackPanel>
    </Grid>
</UserControl>
